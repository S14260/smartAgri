# 遥感 / 巡田服务依赖（api/ndvi，端口 8000）
#
# 版本来源说明：
#   - 带 == 的版本号取自开发机实测环境（Python 3.13.5），可直接安装
#   - gunicorn / flask-cors 开发机上未安装 —— 本服务实际部署在 123.56.228.32 的
#     /www/wwwroot/api，由宝塔面板 + gunicorn 托管（见 gunicorn_conf.py）。
#     这两个包的版本请登上服务器执行 pip freeze 后补齐
#
# 安装：pip install -r requirements.txt
# 启动：python app.py

Flask==3.1.3
flask-cors
gunicorn

# 遥感影像与空间分析
rasterio==1.4.3
geopandas==1.1.1
shapely==2.1.2
pyproj==3.7.2

# 数据处理与绘图
numpy==2.3.4
pandas==2.3.3
scipy==1.16.3
matplotlib==3.10.7
Pillow==12.0.0

# HTTP 客户端（拉取远程 NDVI 影像）
requests==2.32.5
